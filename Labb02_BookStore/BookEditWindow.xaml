<Window x:Class="Labb02_BookStore.Presentation.BookEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Labb02_BookStore.Presentation" xmlns:viewmodels="clr-namespace:Labb02_BookStore.Presentation.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="BookEditWindow" Height="450" Width="800">

    

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
       

        <TextBlock Text="ISBN:" Grid.Row="0" Grid.Column="0" />
        <TextBox Text="{Binding SelectedBook.Isbn13, Mode=TwoWay}" Grid.Row="0" Grid.Column="1"/>

        <TextBlock Text="Title:" Grid.Row="1" Grid.Column="0" />
        <TextBox Text="{Binding SelectedBook.Isbn13Navigation.Title, Mode=TwoWay}" Grid.Row="1" Grid.Column="1"/>

          <TextBlock Text="Price:" Grid.Row="2" Grid.Column="0" />
        <TextBox Text="{Binding SelectedBook.Isbn13Navigation.Price, Mode=TwoWay}" Grid.Row="2" Grid.Column="1"/>

        <TextBlock Text="Balance:" Grid.Row="3" Grid.Column="0" />
        <TextBox Text="{Binding SelectedBook.Balance, Mode=TwoWay}" Grid.Row="3" Grid.Column="1"/>

        <TextBlock Text="DateOfIssue:" Grid.Row="4" Grid.Column="0" />
        <TextBox Text="{Binding SelectedBook.Isbn13Navigation.DateOfIssue, Mode=TwoWay}" Grid.Row="4" Grid.Column="1"/>

        <TextBlock Text="Language:" Grid.Row="5" Grid.Column="0" />
        <TextBox Text="{Binding SelectedBook.Isbn13Navigation.Language, Mode=TwoWay}" Grid.Row="5" Grid.Column="1"/>

        <Button Content="Save Book"
                Grid.Row="6"
                Grid.Column="1"
                HorizontalAlignment="Right"
                Width="120"
                Margin="0,10,0,0"
                Command="{Binding SaveBookCommand}" />
        </Grid>

</Window>
